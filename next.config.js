/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
        domains: ['media.graphassets.com','raw.githubusercontent.com']
  },
  reactStrictMode: true,
}

module.exports = nextConfig
